info:
  name: Post create Todo
  type: http
  seq: 3

http:
  method: POST
  url: http://localhost:3000/todo
  body:
    type: json
    data: |-
      {
        "task": "Learn Bruno",
        "done": false
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
